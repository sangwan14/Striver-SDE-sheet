/* Pre order traversal

  class Solution {
public:
    TreeNode* prev = NULL;
     void flatten(TreeNode* root){
        if(!root) return;
        prev = root;
        flatten(root->left);
        TreeNode* temp = prev; 
        flatten(root->right);
        if(root->left){
            temp->right = root->right;
            root->right = root->left;
            root->left = NULL;
        }
    }   
};

*/

/* Post order traversal
  
  class Solution {
public:
    TreeNode* prev = NULL;
     void flatten(TreeNode* root){
        if(!root) return;
        last = root;
        flatten(root->left);
        TreeNode* temp = prev; 
        flatten(root->right);
        if(root->left){
            temp->right = root->right;
            root->right = root->left;
            root->left = NULL;
        }
    }  
};

*/
